services:
  mysql:
    image: mysql:8.0
    container_name: ticket-mysql
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: rootpassword
      MYSQL_DATABASE: ticket_db
      MYSQL_USER: ticket_user
      MYSQL_PASSWORD: ticket_password
    volumes:
      - mysql_data:/var/lib/mysql

    healthcheck:                      # 🔹 NEW
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      interval: 10s
      timeout: 5s
      retries: 5 

  backend:
    build:
      context: .                      # project root — gives Dockerfile access to frontend/
      dockerfile: backend/Dockerfile  # Dockerfile still lives inside backend/
    container_name: ticket-backend
    restart: always
    depends_on:
      mysql:
        condition: service_healthy

    ports:
      - "443:3000"   # HTTPS  — visit https://localhost
      - "80:3001"    # HTTP redirect → HTTPS automatically

    env_file:
      - .env
    environment:
      DB_HOST: mysql
      HTTPS_PORT: 3000          # port INSIDE the container
      HTTP_PORT: 3001           # redirect port INSIDE the container
      SSL_KEY_PATH: /app/certs/server.key
      SSL_CERT_PATH: /app/certs/server.crt

    volumes:
      # Mount certs as read-only; never bake secrets into an image
      - ./certs:/app/certs:ro

    command: ["node", "src/server.js"]

volumes:
  mysql_data: {}
